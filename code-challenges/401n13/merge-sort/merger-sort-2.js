'use strict';

function mergeSort(items) {
  // Find the midpoint and split the items into two sides
  // Call mergeSort on the left side
  // Call mergeSort on the right side
  // Merge the two halves
}